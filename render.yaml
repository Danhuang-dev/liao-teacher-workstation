services:
  - type: web
    name: liao-teacher-ai-analysis
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python ai_analysis_server.py
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: 8890
